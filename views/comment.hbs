<div class="row">
        <div class="col-md-2"></div>
            <div class="col-md-8 col-sm-12">
                <div class="card mt-5">
                    <div class="card-header">
                        Ezz aldin
                    <form action="/remove/{{post._id}}" method="POST">
                            <button class="btn btn-outline-danger ed">
                                <i class="fas fa-trash"></i>
                            </button>
                    </form>
                        
                        
                    </div>
                <div class="card-body">
                    <p class="card-text">
                        {{post.description}}
                        <a href="/edit/{{post._id}}" class="btn btn-outline-secondary ed1">
                            <i class="fas fa-edit"></i>
                        </a>
                    </p>
                    
                 </div>
                </div>
            </div>
        </div>
            <div class="row posts">
        {{#each comments}}
                <div class="col-md-8 col-sm-8 mb-3 ml-5">
                    <div class="card mt-5">
                        <div class="card-header">
                            Comment
                        </div>
                    <div class="card-body">
                        <p class="card-text">
                            {{this}}
                        </p>
                     </div>
                    </div>
                </div>
            {{/each}}
            </div>
        <div class="container">
            <div class="row">
            <div class="col-md-2"></div>
                <div class="col-md-8 col-sm-12">
                    <div class="card border mt-5">
                        <div class="card-header">Creat Comment</div>
                        <form action="/upload/{{post._id}}" method="POST">
                            <div class="container">
                                <div class="form-group">
                                  <label for="Body">Comment Body</label>
                                  <textarea class="form-control" name="commentBody" id="Body" rows="5" placeholder="comment body" required></textarea>
                                </div>
                                <div class="form-group">
                                        <button type="submit" class="btn btn-dark rounded-pill creat-post1"> 
                                            Create Comment
                                        </button>
                                    </div>
                            </div>
                              </form>
                        
                     </div>
                </div>
            </div>
        </div>